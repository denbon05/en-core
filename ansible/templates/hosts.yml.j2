      hosts:
{% for ipv4 in infra.outputs.server_ips.value %}
        web-server-{{loop.index}}:
          ansible_host: {{ipv4}}
          ansible_user: ubuntu
{% endfor %}

    {# load_balancer:
      hosts:
        ansible_host: {{infra.outputs.app_lb_public_ip.value}} #}
